function MainTitle({ title, breadcrumb }) {
  return (
    <div className="w-auto flex justify-center mt-16">
      <h1 className="text-5xl font-bold capitalize">{title}</h1>
    </div>
  );
}

export default MainTitle;
